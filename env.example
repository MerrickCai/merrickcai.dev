# ─── Server-side only (without NEXT_PUBLIC_ prefix) ────────────────

# Upstash Redis database REST API endpoint
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io

# Upstash Redis database REST API token
UPSTASH_REDIS_REST_TOKEN=your_redis_rest_token_here

# ─── Client-side visible (NEXT_PUBLIC_ prefix) ──────────────────

# Base URL for the application (production environment)
NEXT_PUBLIC_BASE_URL=https://your-domain.com